/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.something.app;

import com.something.list.LinkedList;

import static com.something.utilities.StringUtils.join;
import static com.something.utilities.StringUtils.split;
import static com.something.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
